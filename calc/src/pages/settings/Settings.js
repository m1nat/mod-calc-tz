import Settings from "../../components/Settings/Settings"


const Settings_page  = ({themeToggle}) => {
  return (
    <div>
      <Settings themeToggle={themeToggle}/>
    </div>
  )
}

export default Settings_page;